import dash_bootstrap_components as dbc

carousel = (
    dbc.Carousel(
        id="carousel",
        items=[],
        variant="dark",
        controls=True,
        indicators=True,
        interval=None,
    ),
)
